services:
  web:
    build: ./web
    command: python manage.py run -h 0.0.0.0
    volumes:
      - ./web/app/birdie_booker/alerts.db:/app/app/birdie_booker/alerts.db
    ports:
      - 5001:5000
    env_file:
      - ./.env.dev
